extends base.pug
include mixins.pug

block page-body
		form#form(method='POST' action=base_route+data_route)
			div#formDiv
				+displayObj(schema)
			input.submit(type='submit',value='Envoyer le formulaire')




						
			
				